# 统一行尾：仓库内一律 LF，避免 Windows 开发机与 CI / 容器之间反复出现
# 「整文件改动」以及 prettier --check 因 CRLF 失败。
* text=auto eol=lf

# 二进制文件不做行尾转换
*.png binary
*.jpg binary
*.ico binary
*.db binary
*.db-wal binary
*.db-shm binary
